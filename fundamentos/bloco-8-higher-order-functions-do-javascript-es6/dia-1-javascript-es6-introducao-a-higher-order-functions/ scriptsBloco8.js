// //exercicio 1

// const employeeData = (nomeCompleto) => {
//   const email = nomeCompleto.toLowerCase().split(" ").join("_");
//   return { nomeCompleto, email: `${email}@trybe.com` };
// };

// const newEmployee = (employeeData) => {
//   const employees = {
//     id: employeeData("Giordano Mendes"),
//   };
//   return employees;
// };

// console.log(newEmployee(employeeData));

// //Exercicio 2;

// const betting = (betNumber, drawnNumber) => betNumber === drawnNumber;

// const lotery = (drawnNumber, callback) => {
//   const drawnNumber = Math.floor(Math.random() * 10);

//   return callback(betNumber, drawnNumber)
//     ? "Lucky day, you won!"
//     : "Try Again!";
// };

// Exercicio 3
